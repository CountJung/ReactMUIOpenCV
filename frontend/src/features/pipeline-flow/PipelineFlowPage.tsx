import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

export function PipelineFlowPage() {
  return (
    <PlaceholderPage
      title="Pipeline Flow"
      eyebrow="React Flow"
      description="Compose OpenCV processing nodes and execute image or video pipelines."
    />
  );
}
