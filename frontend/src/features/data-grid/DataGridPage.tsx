import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

export function DataGridPage() {
  return (
    <PlaceholderPage
      title="Data Grid"
      eyebrow="Tables"
      description="Job history, file library, and result records will be browsed in structured tables."
    />
  );
}
